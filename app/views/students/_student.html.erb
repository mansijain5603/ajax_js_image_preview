<div id="student_<%= student.id %>">
<li>
     Name:<%= link_to student.first_name, student_path(student) %>
     <% if student.images.attached? %> 
          <% student.images.each do |image| %>
     <%= image_tag image %>
    <% end %>
    <% end %>
</li> 
</div>